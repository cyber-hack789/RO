10.Write a program to create  an any application of Linear Regression in 
multivariate context for predictive purpose. 

#data
data<- data.frame(
    Experience=c(1,2,3,4,5),
    age=c(20,22,23,24,25),
    salary=c(20000,30000,40000,50000,60000)
)

model<-lm(salary ~ Experience+age, data)

new <- data.frame(Experience = c(6,7), Age = c(32,35))
pred<-predict(model,new)

cat("dataframe:\n")
print(pred)