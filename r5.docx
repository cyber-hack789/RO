5.Write an R program for calculating cumulative sums, products, minima, 
maxima, and calculus.
# Cumulative
x <- c(2, 4, 6, 8, 10)
cat("Cumulative Sum:", cumsum(x), "\n")
cat("Cumulative Product:", cumprod(x), "\n")

# Minimum and Maximum
cat("Minimum Value:", min(x), "\n")
cat("Maximum Value:", max(x), "\n")

# Calculus
cat("Derivative of x^2:\n")
print(D(expression(x^2), "x"))

cat("Integration of x^2 (0 to 3):\n")
print(integrate(function(x) x^2, 0, 3)$value)